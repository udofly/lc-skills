# 检查点：登录重构

- **日期**：2026-07-14
- **分支**：`feature/login-refactor`
- **基线提交**：`5294fcc` init: scaffold auth module

## 本次工作内容

登录模块重构，未提交改动共 2 个文件：

| 文件 | 状态 | 说明 |
| --- | --- | --- |
| `src/auth.ts` | 修改 | `login()` 改为接收 `token: string` 参数，内部调用新增的 `verify()`；新增 `verify(t: string)` 校验函数 |
| `src/session.ts` | 新增（已 git add） | 定义 `SESSION_TTL = 3600` |

### 关键代码现状

```ts
// src/auth.ts
export function login(token: string) {
  return verify(token)
}
export function verify(t: string) {
  return t.length > 0
}
```

```ts
// src/session.ts
export const SESSION_TTL = 3600
```

## 已定决策

- token 校验先做**长度非空的占位实现**（`t.length > 0`），后续替换为 JWT 校验。

## 剩余工作（TODO）

1. [ ] `verify()` 接入真实 JWT 库，替换占位实现
2. [ ] `src/session.ts` 增加过期刷新逻辑（基于 `SESSION_TTL`）
3. [ ] 补单元测试（`login` / `verify` / session 过期刷新）

## 恢复方法

```bash
cd /tmp/lc-cp-fixture-save-baseline
git checkout feature/login-refactor
git status   # 应看到 src/auth.ts 修改、src/session.ts 已暂存
```

从「剩余工作」第 1 项继续：给 `verify()` 接 JWT 库。

## 保存时 git 状态

```
 M src/auth.ts
A  src/session.ts
```
