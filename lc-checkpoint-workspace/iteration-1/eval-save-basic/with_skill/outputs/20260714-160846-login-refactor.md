---
status: in-progress
branch: feature/login-refactor
timestamp: 2026-07-14T16:08:46+08:00
files_modified:
  - src/auth.ts
  - src/session.ts
---

## Working on: 登录重构

### Summary

重构登录模块：`login()` 从原有签名改为接收 token，并新增 `verify()` 校验函数；同时新增 `src/session.ts` 定义 `SESSION_TTL` 会话常量。核心改动已完成（auth.ts 已改、session.ts 已 staged），校验逻辑目前是占位实现。

### Decisions Made

- **token 校验先做占位实现（仅校验长度非空）**：为了先打通登录链路，暂不引入 JWT 依赖；后续替换为真实 JWT 校验。
- **会话常量独立成文件**：新增 `src/session.ts` 承载 `SESSION_TTL`，与认证逻辑解耦，为后续过期刷新逻辑留位置。

### Remaining Work

1. `verify()` 接入真实 JWT 库，替换当前"长度非空"的占位校验
2. 给 `src/session.ts` 补充会话过期刷新逻辑
3. 补单元测试（覆盖 login/verify 与 session 过期）

### Notes

- 当前 git 状态：`src/session.ts` 已 staged（新增 1 行），`src/auth.ts` 未 staged（+5/-2），均未提交。
- 占位校验上线前必须替换为 JWT，否则存在安全隐患。
